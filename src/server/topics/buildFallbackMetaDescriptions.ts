export const buildFallbackMetaDescriptions = (title: string) => {
  return [
    `A clear guide to ${title.toLowerCase()}, with practical tips and questions to help readers decide what to do next.`,
    `Learn what matters with ${title.toLowerCase()} so readers can make a confident choice without digging through search results.`,
  ];
};
